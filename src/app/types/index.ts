export interface CloudinaryResource {
    bytes: number;
    created_at: string;
    format: string;
    width: number;
    height: number;
    public_id: string;
    secure_url: string;
    tags: Array<string>;    
}